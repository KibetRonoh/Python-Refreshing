# You are building a Turing system with Django using the following source code?

CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends .db.DatabaseCache',
        'LOCATION': 'data_cache'
    }

    # what is the target of the above settings?
}